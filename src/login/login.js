// put login logic here.

// very basic middleware.
// query database to see if slack_user has non-expired token in database...
// if yes - carry out command - if no ask for credentials and then set fresh token in database.


// export const isLoggedIn
